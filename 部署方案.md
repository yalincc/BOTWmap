# BOTWmap 部署方案

> **已部署（2026-09-12）**：腾讯云 EdgeOne Pages（Makers）+ 自定义域名 **https://botw.yalin.site/**（全球可用区不含中国大陆，无需备案）。
> 部署方式：EdgeOne 连接 Gitee 仓库 `yalincc/botwmap`（main 分支自动部署），输出目录 `app`，免费证书自动签发。

> 纯静态项目（HTML + JS + 图片，零后端、零构建），**部署只需要 `app/` 一个目录（约 42MB）**。
> 地图瓦片 / 图标 / 数据全部本地，无需任何服务器逻辑，任何静态托管都能跑。

## 部署前须知

- **体积**：`app/` = 41.8MB（瓦片 41MB + 图标 0.1MB + 代码 <1MB）；10096 张瓦片按需加载，单张 1~8KB
- **完成进度 / 自定义标记 / 图层选择**：存在浏览器 `localStorage`，按**域名**隔离——部署后访问者的进度存在各自浏览器，换设备不互通（如需云同步属后续功能）
- **分享链接**：用 URL `#` 参数记录图层 + 视图位置，纯前端解析，部署后照常可用
- **路径**：`index.html` 内全部相对路径引用（`js/` `css/` `assets/`），上传时保持目录结构不变即可

---

## 方式一：局域网（最快，零成本，手机同 Wi-Fi 可访问）

```bash
cd E:\WorkSpace\BOTWmap\app
python -m http.server 8080
```

- 本机访问：`http://127.0.0.1:8080`
- 手机 / 其他电脑：`http://<本机局域网IP>:8080`（查 IP：`ipconfig`，Windows 防火墙首次需允许）
- 适合：自己多设备用、朋友局域网点开；关机即停

## 方式二：国内云 / 对象存储（公网访问，速度快）

任选其一：

| 平台 | 操作要点 | 成本 |
|---|---|---|
| 腾讯云 COS | 上传 `app/` 内容 → 开启「静态网站托管」→ 得 `xxx.cos-website.ap-xxx.myqcloud.com` 域名；可绑自定义域名 + CDN | 按量付费（几元/月级） |
| 阿里云 OSS | 同上：上传 → 静态网站托管 → 绑定域名 | 按量付费 |
| 轻量服务器（宝塔） | 服务器装宝塔面板 → 新建站点，根目录指向 `app/` 上传内容 → 得公网 IP 域名 | 几十元/月 |

建议：对象存储 + CDN 最省心（静态文件天然适合），放量小可忽略费用。

## 方式三：国外免费托管（0 元，国内访问速度一般）

| 平台 | 说明 |
|---|---|
| GitHub Pages | 仓库推 `app/` 内容到 `gh-pages` 分支或子目录；国内访问时好时坏 |
| Cloudflare Pages | 免费额度足，国内连通性相对较好 |
| Netlify / Vercel | 拖拽上传即可，国内访问一般 |

均支持：git 推送或网页拖拽上传，自动 HTTPS。

---

## 部署后自检清单

- [ ] 打开首页：全图秒开（zoom2 瓦片），无 404（F12 Console 无 `Failed to load resource`）
- [ ] 放大到最大（1:1）：地图清晰，瓦片逐级加载
- [ ] 勾选 / 分组勾选图层：地图同步变化
- [ ] 搜索跳转、点击卡片、分享链接（复制给别人打开同款画面）
- [ ] 刷新后完成进度仍在（localStorage 生效）

## 可选优化（上线后）

- 开启 HTTP/2（瓦片小文件多，明显提速）+ gzip/brotli（对 JS/HTML 有效，图片本身已压缩）
- 瓦片已转 WebP（41MB），无需再压；如需更小可把 q82 降到 q75（省约 1/4，肉眼差异极小，用 `tools/convert_tiles_webp.py` 重转）
