# 更新日志

本项目遵循语义化版本（SemVer）：`主版本.次版本.修订号`。

## v1.1.9（2026-09-22）

### 新增
- **探索度卡片弹层**：侧栏「探索度」改为按钮（▤ 探索度），点击弹出完整统计卡片（PC 居中 / 手机底部抽屉），看全 9 组统计 + 进度条，不再被侧栏高度截断；Esc / 点遮罩 / × 关闭
- **图层进度统一显示**：所有图层组统一显示 `已完成/总数`（如设施地点 0/13、敌人 0/23、任务 0/16），不再"有标记才显示"造成行与行之间不统一
- **滚动条稳定**：`.layer-list` 开启 `scrollbar-gutter: stable`，材料大类展开/收起时内容宽度不再跳动（修复点击大类文字偏移）；全站滚动条统一细样式

## v1.1.8（2026-09-22）

### 新增
- **侧栏 Tab 重构（Step2）**：移除「图层组合」模块（刷新易丢失、用处不大）；材料 Tab 新增材料专用搜索（按名称搜 74 种材料，命中即展开大类 + 勾选图层 + 飞到分布中心）；材料 6 大类默认全部展开（去掉手风琴互斥，可独立收起）；「收集进度」改名「探索度」并支持折叠/展开，状态记忆
- **材料搜索勾选同步**：从搜索结果点击材料后，侧栏对应材料 checkbox 同步勾选（探索/材料两处搜索一致）

## v1.1.7（2026-09-22）

### 新增
- **侧栏 Tab 重构（Step1）**：侧栏拆「探索 / 材料」两个 Tab（CSS 显隐，不重建 DOM，图层/搜索/统计逻辑零改动）；Tab 位置 localStorage 记忆；探索 = 搜索/图层/探索度/存档，材料 = 材料·采集组

## v1.1.6（2026-09-22）

### 修复
- **手机信息卡看不到/拖不动**：手机模式（≤760px）信息卡改为底部抽屉（全宽、可滚动、右上角关闭），修复卡片被误判“侧栏展开”避让 312px 而推出屏幕外的问题
- **微信内置浏览器顶栏标题竖排**：手机顶栏改两行布局（标题独占一行不压缩、工具按钮第二行均分），微信 X5 内核下不再把标题压缩成竖排
- **手机图层按钮点不到**：顶栏修复后 ☰ 图层按钮可点区域恢复，按钮保留图标+文字

## v1.1.5（2026-09-22）

### 修复
- **克洛格轨迹缩放阈值**：轨迹只在放大到区域级别（≥ 全图适配的 2 倍，实际约 0.08）才显示，全图比例不再画满屏折线（此前固定阈值 0.04 低于全图适配的 ~0.045，导致全图也显示轨迹）
- **轨迹命中同步**：点击命中也使用同一缩放阈值，全图下不可见的同时也不可点

### 新增
- **右下角缩放百分比**：全图适配 = 100%，放大 2 倍 = 200%，实时显示在右下角 ＋ 按钮上方

## v1.1.4（2026-09-22）

### 新增
- **克洛格轨迹（追花/赛跑路线）**：97 个多点谜题在地图上直接显示完整路线，黄色折线（#E8C547 深色描边），数据来自 ZDMap `seeds.json`（生成脚本 `tools/build_korok_paths.py` 归一化坐标，按 No 关联 `korok_type_audit.csv`）
- **轨迹开关**：右上角 ⚙️ 设置 →「克洛格轨迹」显示/隐藏，localStorage 持久化，默认显示；缩放 ≥0.04 才画，跟随克洛格图层同显隐
- **点击轨迹选中**：点击折线任意一段可选中对应克洛格，查看详情卡

## v1.1.2（2026-09-21）

### 修复
- **位置追踪漂移/卡死**：修复 BotwNavi 启动时位置在两个地方来回偏移、以及追踪卡住 1 分钟才恢复的问题
  - 恢复快速锁定：玩家移动 1~2 秒内锁定真实坐标槽，红点实时跟随
  - 启动不再锁到 (0,0,0) 附近的零残留内存（近零/极小值坐标过滤）
  - 玩家站桩时不再被误判为"死槽"而反复重新定位
  - 坐标记忆只保留最近 10 条，避免垃圾偏移污染快速定位投票

## v1.1.1（2026-09-19）

### 新增
- **克洛格果实全量标注**：900 个克洛格全部补齐谜题类型（15 类中文大类：搬石头/磁铁·方块拼图/垒石·石圈/射气球/精灵光点/射箭·射靶/食物祭品/追花/赛跑·树桩/跳水环/推石入坑/树叶下/彩色花·按序踩/冰块/其他），地图详情卡显示类型彩色徽章
- **位置截图**：每个克洛格一张游戏实景截图（900 张自托管 `app/assets/korok/`，随网站打包部署，不连接任何外部图床）；打开卡片才加载（懒加载），点击查看原图（灯箱）
- **中文一句话解法**：900 条解法压缩为一句中文提示，完整英文原文保留在数据文件中
- **挑战起始位置**：168 个"起点≠收获点"的克洛格，地图上在挑战起点处显示类型色第二标记 + 收获点连线，详情卡新增「导航到起点」按钮（live 层联动），解决"地图标的是收获点、开启挑战的位置不在这里"的问题

### 优化
- 卡片截图改为按原始分辨率居中显示（不再拉伸发糊），灯箱标注"原图分辨率"

## v1.1.0（2026-09-19）

### 新增
- **材料追踪图层**：全量标注野外可采集材料刷点，覆盖核心 6 类（植物/草药、蘑菇、水果、昆虫、鱼、矿物，共 74 种材料 ≈13,524 个刷点），每种材料独立官方图标 + 选中高亮
- **常用材料三角收起/展开**：与植物大类样式一致，图层可折叠

### 修复
- **常用材料标题始终显示**：材料为空时显示 (0)，不再整栏隐藏

## v1.0.2（2026-09-18）

### 新增
- **最终回忆单独显示**：回忆 #17（照片13 · 萨尔达觉醒）从普通照片回忆中分离，地图上用金色五角星标记，信息卡提示"需找齐 12 张照片后回英帕处激活"
- **照片记忆图层**：左侧图层名改为"照片记忆（12+1）"，统计口径 x/13（12 张照片 + 1 个最终回忆）
- **图标层级调整**：村庄/驿站等地点图标沉底，克洛格/宝箱等收集图标置顶，不再被遮挡

### 修复
- **回忆收集统计与地图图标不同步**：上传存档后，地图上的回忆完成状态、左侧面板统计、收集进度现在统一以存档为准，手动标记不再覆盖存档数据
- **位置追踪失效**：修复重启后位置卡死/跳变的问题，现在会自动重新定位

## v1.0.1（2026-09-18）

### 修复
- **回忆收集统计不准**：修复 23 个回忆的完成状态判定错误，现在照片记忆、最终回忆、主线自动、DLC 四类分别统计
- **位置追踪卡死**：修复 BotwNavi 重启后位置冻结的问题，现在会自动检测并重新定位

## v1.0.0（2026-09-16）

首个正式版本。

- 旷野之息互动地图：多图层同显、搜索、测量、标记、分享、全图、设置（响应式适配手机/PC）
- 角色追踪：本地 Go 工具 BotwNavi 读取 Ryujinx 内存实时定位
- 导航/收集模式：回忆/克洛格连续自动导航、到达提示、标记完成
- 收集进度：存档同步、进度统计面板、本地进度持久化
