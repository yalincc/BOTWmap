# BOTWmap 优化建议

> 2026-09-12 运行验证后提出 · 仅此 2 项 · **均已实现（2026-09-13 确认）**，本文档保留作历史记录

## 1. 默认勾选改为「神庙 + 希卡塔」

**为什么**：现在默认勾了 9 类（神庙、希卡塔、神兽、克洛格果实、回忆影片、驿站、村庄、古代研究所、大精灵泉），首屏图标叠成一堵墙，看不清也点不中。参考站 zelda.ali213.net 在低缩放级别只显示塔 + 地名，首屏非常干净。

**改哪里**：`app/js/ui.js` 第 444 行

```js
// 现状
const def = ['shrine', 'tower', 'beast', 'seed', 'memory', 'stable', 'village', 'lab', 'fountain'];

// 改为
const def = ['shrine', 'tower'];
```

**⚠️ 必看的坑**：`def` 只在 localStorage **没有记录时**才生效。凡是访问过本站的用户，本地都存了 `botwmap.layers.v1`，改了默认值他们看不到效果。二选一：

- **推荐**：把 key 升版本——`ui.js:435` 的 `botwmap.layers.v1` 改成 `v2`，新默认对所有人生效一次（代价：用户旧的图层选择被重置一次，可接受）
- 或：不动 key，只对新用户生效

**优先级**（不要改错顺序）：分享链接 `#l=` > localStorage > `def`。改默认值不影响分享链接功能。

**验收**：无痕窗口打开 → 首屏只有神庙 + 希卡塔两类图标；侧栏复选框状态同步；分享链接、`全选/清空` 按钮仍正常。

---

## 2. favicon.ico 404

**现象**：控制台唯一的报错 —— `GET /favicon.ico 404`（浏览器自动请求，页面没提供）。

**改哪里**：`app/index.html` 的 `<head>` 内加一行（内联 data-URI，不新增文件、file:// 下也生效、部署不用额外上传）：

```html
<link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'><text y='26' font-size='26'>🗺️</text></svg>">
```

注意：外层属性用双引号，SVG 内部用单引号。若担心 emoji 在不同系统渲染不一致，可换纯几何图形（如一个实心圆 + 十字）。

**验收**：F12 控制台不再出现 favicon 404，Network 里该请求消失或返回 200。

---

## 备注：不需要做性能优化

实测数据（hook `draw()` 计时）：默认勾选 1111 点 = **1.54ms/帧**；全开 28 类 3084 点全画 = **4.19ms/帧**，而 60fps 预算是 16.7ms —— 桌面端远没到瓶颈。空闲 3 秒 `draw()` 调用 **0 次**（已是事件驱动的按需渲染，无需加脏标记）。

所以**不要**顺手加图标分级显隐、抽稀聚类、渲染节流之类的改动，当前架构没问题。
