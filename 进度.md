# BOTWmap 进度

> 人 + AI 共看，开工前必读。规则见 `E:/WorkSpace/AI-CORE/`（RULES / WORKFLOW / PROJECT-INDEX）。

> **一句话**：《旷野之息》本地互动地图复刻——gamertw 全量数据 + ali213 图标/瓦片底图，多图层 + 收集追踪 + 搜索 + 测量 + 地名层，零依赖纯静态。
> **状态**：已完成（2026-09-13 最新提交 `653fbcc` 已推双远端，EdgeOne 已自动部署）
> **技术栈**：原生 HTML + Canvas 2D + JS（零构建零后端）；Python（数据处理 / 瓦片）；浏览器自动化验证
> **路径**：`E:\WorkSpace\BOTWmap`（产品入口 `app/index.html`）
>
> **新模块（live → live-go）**：角色追踪已重构为 Go 版独立工具（`live-go/`，2026-09-15 提交 `b3f0b67` 已推双远端），在线站跨域连本地服务，见下「最近进展」。

## 一句话

《旷野之息》本地互动地图复刻——合并 gamertw 全量数据（3084 点 / 28 类 / 全简中）+ ali213 图标 / 神庙信息 / 瓦片底图，多图层同显 + 收集追踪 + 搜索 + 测量 + 自定义标记，纯静态零依赖。

## 状态

- **已完成**（2026-09-12 老大确认"暂时完成"；档案已回填 `PROJECT-INDEX.md`）
- **已部署**：https://botw.yalin.site/（EdgeOne Pages + Gitee 自动部署，2026-09-12）
- 最终产物：`app/`（`index.html` 双击即开，无需服务器、无需联网）
- ✅ **全部未提交改动已提交并推送**（`653fbcc`，2026-09-13，gitee + GitHub）：地名译名全面修正 + 地名分级弱化保留 + 手机/PC 缩放统一 + 优化建议 2 项。详见「最近进展」

## 项目要点

- **数据**：gamertw 3084 点 / 28 类全简中；ali213 神庙试炼 / 宝藏 / 任务信息；坐标统一 24000 系（`gameX=(pxX-12000)/2`）
- **引擎**：Canvas 瓦片动态加载（10096 张 WebP 41MB，zoom1~7 同 ali213）、LRU 缓存、最大 1:1 像素封顶
- **功能**：35 张游戏内图标固定像素、分组一键勾选、神兽坐标修正、完成度统计、分享链接
- **体积**：690MB → 75MB（source 构建产物已清入回收站，可用 `tools/` 脚本重建）

## 最近进展（v1.0 之后，2026-09-12 下午~晚上）

> 进度.md 上次更新是 11:59，以下为之后的 5 次提交 + 未提交改动（归档时补记）

1. `4764cfd` 手机版侧栏改抽屉式（☰ 展开 / 遮罩收起，参考 ali213）
2. `b74c8be` 手机端触摸手势（单指拖拽 / 双指捏合 / 双击放大），缩放上限 = 物理像素 1:1（dpr）
3. `566e24e` 瓦片降级兜底（放大不空白，用低一级瓦片撑画面），加载限流 24→48
4. `8e826e6` 缩放上限改为物理密度对齐 `maxScale=1/dpr`——手机最大清晰度与电脑一致，消除超分模糊
5. `de0c104` 地形/区域名文字层（8 大区 + 21 主要地形区，简体中文官方译名，半透明白底深字仿游戏内样式，随缩放分级切换）
6. 设置弹层 ⚙️ —— 地名文字颜色 / 大小 / 描边（颜色+粗细）/ 底色 + 各自透明度滑块；`regions.js` 地名数据扩到 r1 大区 / r2 地形区 / r3 细地名 / r4 更细地名（以上并入 653fbcc 一起提交）

## 最近进展（2026-09-13：地名系统重构，提交 `653fbcc`）

- **地名译名全面修正**：r3 362 + r4 94，以游戏官方简体为准（数据源 `LocationMarker.xmsbt` 官方名 + `Location.xml` 官方坐标 + ali213 中文兜底 + MANUAL 手工表）。约 260 条改名：特拉平原→玛贝大草原、拉沙玛平原→北海拉鲁平原、卡卡奥梅平原→伊尔奇平原、隆隆河→海利亚河、海利亚河→死亡之河、特拉卡×9 岛→各自官方名、翁吉森林→艾普林森林 等。重名仅剩「大妖精泉」×4（刻意保留）
- **地名分级改「整数倍阈值 + 瓦片清晰度兜底」**：r1 全图~2x 后隐藏；r2 2x 起显（r3 出现后**隐藏**）；r3 4x 起显（r4 出现后弱化保留 α×0.8、字号封顶 1.7）；r4 8x 且瓦片 z≥6 起显（字号上限 2.0）。字号封顶解决"二三级字体过大"
- **手机/PC 缩放统一 `maxScale=1`**：手机也能放大到 z7 最高清（之前 1/dpr 只能 z5，且 r4 需 z6 才显示——是"手机放大模糊"根因）；LRU 缓存兜底内存
- **优化建议 2 项**（`优化建议.md`）：默认图层改 神庙+塔（`botwmap.layers.v2`）；index.html 内联 favicon 修 404
- `regions.js` 引用改 `?v=rn3` 绕浏览器缓存
- 验证：bu 逐级断言（r1→r2→r3→r4）+ 像素对比 + 控制台零报错，均通过

## 最近进展（2026-09-13：图层组合 + 地名显示开关）

- **图层组合**（侧栏「图层与搜索」面板新增「图层组合」区）：把当前勾选的图层存成自定义组合（localStorage `botwmap.combos.v1`），点组合名一键应用、× 删除，组合内已删除的图层应用时自动忽略。例：存「神庙+塔」「人马+巨人」，来回切换不用逐个勾
- **地名显示开关**（⚙️ 设置弹层顶部「地名显示：显示/隐藏」）：控制 `map.showRegions`，`_drawRegions` 判空跳过；独立持久化 `botwmap.regionShow.v1`；「恢复默认」一并复位为显示
- 改动 4 文件：`index.html`（控件）、`ui.js`（组合 + 开关逻辑）、`map.js`（showRegions）、`style.css`（组合区/开关样式）；`map.js?v=3` `ui.js?v=3` 绕缓存
- 验证：bu 全流程（保存/应用/删除/刷新持久化/开关地名/恢复默认）+ Chrome headless 390px 手机视口（抽屉 ☰、组合在抽屉内可用）+ 控制台零报错，均通过

## 2026-09-16：导航「收集模式」— 回忆 / 克洛格连续自动导航（提交 a1e1f55）

> 用户需求：原导航一次只能选一个目标、到达后要手动点下一个；新增收集模式让回忆/克洛格能连续自动导航。

- **纯前端 4 文件**（`live.js` / `ui.js` / `index.html` / `style.css`），Go 服务与数据零改动（复用 /target + /pos）
- **标注卡「收集模式」开关**（仅回忆/克洛格卡片显示，离线置灰）：开启后克洛格=点哪个导航哪个；回忆=最近的未收集**主线回忆**（13 个有存档 flag，EX/英杰回忆不进自动队列，仍可手动单点导航）
- **自动推进**（每 600ms 轮询，复用存档同步链路：游戏内保存 → 服务 ~2s 检测 → progressGen 变化 → 前端拉取 → liveDone 更新）：当前目标已收集（存档同步或手动勾选）→ 自动设下一个 = 离玩家最近的未收集同类点；全部收完 → toast + 自动退出并清除目标
- **到达提示**（距离 ≤ 15m）：横幅「已到达 · 收集后自动前往下一个」，只提示不跳转（防未解谜就被带走、防两点来回乒乓）；走远 >22.5m 提示复位
- **底部横幅**：类别 + 已收集 d/t + 当前目标名 + 「结束」按钮；退出=结束按钮 / 手动导航其他类别 / 服务离线自动暂停（恢复连接后继续）；同类别手动导航=重定向并保持模式
- **验证**：桩服务（Node 模拟 live-go API）全链路浏览器实测——开启、收集后自动推进（No.004→No.137→No.069 与独立计算一致）、到达提示、远离复位、全收完自动退出、结束按钮、异类导航退出、离线暂停/恢复、手机断点 CSS 核验、控制台零报错
- 版本号：live.js?v=8 / ui.js?v=9 / style.css?v=11；帮助弹窗补「收集模式」说明
- **UI 迭代**（提交 9bd6196）：收集模式开关改为卡片按钮行的「收集」按钮（导航/收集/标记完成三按钮统一——去图标、等宽、分层配色：导航绿实底/收集金/标记完成中性）；信息卡不再固定左下角，改为跟随点击点弹出、桌面侧边栏展开时自动避让、可按住头部拖拽移动（手机触摸同样生效）；版本号 css v13 / ui v11 / live v9

记录时间：2026-09-16（导航收集模式实施完成）

## 停在哪 / 下一步

- ✅ **待办 1 已完成**（2026-09-13）：`653fbcc` 已提交并推送 gitee + GitHub（触发 EdgeOne 自动部署，线上 https://botw.yalin.site/ 已更新）
- 仓库双远端：GitHub `yalincc/BOTWmap`、Gitee `yalincc/botwmap`（EdgeOne 自动部署源）
- 可迭代方向：更多自定义图层、攻略数据联动、离线数据更新
- **可清理**（未擅自删，老大确认后处理）：根目录 `.preview/`、`.tmp_ali213.html`、`.workbuddy/`、`preview.png`；`source/` 下 `dbg_*` / `check_*` 调试脚本与 `user_*` / `v_*` / `stv*` / `verify_*` / `cz4x` / `r2hide` / `rn3_castle` 等临时截图（约 45 个 png）；`source/_进度backup.txt`（归档备份，可删）

## 踩坑速记（详细见 PROJECT-INDEX 与知识库「BOTWmap-大图瓦片化性能方案」）

1. 大图整图常驻内存（45MB → 1.4GB）拖拽卡 → 瓦片动态加载解决（**性能优化先查参考实现机制**）
2. PIL 拼超大图要 `Image.MAX_IMAGE_PIXELS=None`；WebP 编码器 16383px 上限
3. file:// 下 canvas 被污染（getImageData 抛 SecurityError），验证靠页面状态 + 截图

## 目录速览

```
app/             65MB   产品本体（部署只需这个；瓦片 10096 张 WebP）
source/          91MB   原始数据 JSON + 构建 / 调试脚本
tools/          100KB   数据构建 / 瓦片抓取 / 验证脚本
waypoint-data/   52MB   第三方参考克隆（MrCheeze/botw-waypoint-map，.gitignore 已排除）
.git/            55MB
```
实扫时间：2026-09-12 22:0x；总计约 263MB

---

记录时间：2026-09-12 22:00（归档实扫更新：补 v1.0 后 5 次提交 + 未提交改动、磁盘实扫体积、待清理清单）

---

## 2026-09-13 地名简体中文官方校对

### 完成内容
- 从游戏 RomFS 的 `Bootup_CNzh.pack` 中提取了官方简体中文地名（`LocationMarker.msbt`，692 条）
- 与项目 `regions.js` 中的 488 条地名按 label 匹配校对
- **327 条地名已修正为游戏官方简体中文译名**（如：达布池→达鲁波池塘、格尔克湖→马尔鼓池塘、戈特拉姆悬崖→托洛鼓悬崖等）
- 原文件已备份为 `regions.js.bak`

### 未完成项
- r4 层 91 条 label 为 `???` 的细地名（城堡内部、遗迹、特殊地点等），这些不在 `LocationMarker.msbt` 里，暂时保留原名，后续单独核对

### 产出文件
- `source/官方地名提取指南.md` — 从游戏文件提取官方地名的完整流程文档
- `source/简体中文地名表.csv` — 游戏官方简体中文地名（692 条，label→中文名）
- `source/地名校对表_纯净版.csv` — 项目地名 + 官方名对照表（488 条）
- `source/parse_msbt.py` — MSBT 解析脚本
- `app/js/regions.js` — 已更新（327 条地名修正）
- `app/js/regions.js.bak` — 原文件备份

记录时间：2026-09-13 21:30（地名官方校对：提取游戏简中地名、修正 327 条译名）

---

## 2026-09-13 官方坐标全量校对（Location.smubin + Static.smubin）

### 坐标来源（本轮新增，官方 ROM 准确位置）
- 坐标不在地上分块瓦片，而在 `Pack/Bootup.pack`（SARC）：
  - `Map/MainField/Location.smubin`（= waypoint-data/Location.xml，491 条记录 / 398 唯一 label）
  - `Map/MainField/Static.smubin`（= waypoint-data/Static.xml：LocationMarker 215 + LocationPointer 131 + KorokLocation 900 + SaveFlag 307）
  - 官方总表 706 个唯一 label（含 ID 别名桥接），换算 px = (2*X+12000, 2*Z+10000)
- 解包路径：`G:\YUZU\Switch Games\RomFS\Pack\Bootup.pack`，中文名仍取 `LocationMarker.msbt`（692 条）

### 比对结果（488 条，双策略：中文名唯一匹配 → 坐标近邻 <15 游戏单位）
- 无需改 396；改名 41；改名+改坐标 39；改坐标 3（含「迷失的森林」偏移 617.8px → 官方 LostForest）；坐标改官方、名字待定 3
- 合计应用 **86 条** 官方坐标/名称修正，写入 `app/js/regions.js`（应用前备份 `regions.js.bak2`）
- 名字待定 8 条（官方坐标已对齐但 MSBT 无中文，保留原名）：TabantaSnow / EastGerudo / EunpoHighlands / TrakaIsland / HyruleCastleTown / WeaponCureSpring_02/03/04
- **官方未收录 1 条（不动）**：r4 采石场遗址 px(14671,13667)，单独列出待定
- 重名：新增 10 组 + 大妖精泉×3 = 11 组，沿用 r4 弱化保留逻辑

### 验证
- 重跑比对脚本：487 条匹配官方 / 1 未收录 / 0 待改；node --check 通过
- 浏览器运行时：488 条加载、迷失的森林=官方坐标、11 组重名、渲染源=regions.js、控制台零页面报错

### 产出文件（source/）
- `build_official_fix.py` — 官方表构建 + 双策略比对（幂等，可重跑）
- `apply_official_fix.py` — 修正应用脚本（按 lv+现名+坐标主键精确匹配）
- `官方修正清单.csv` — 487 条最终核对版（重跑后：无需改 479 + 名字待定 8）
- `官方修正清单_应用前.csv` — 86 条修正应用前基线
- `官方未收录清单.csv` — 1 条（采石场遗址）
- `官方地名坐标总表.csv` — 706 label 坐标总表（含中文名/game_x/game_z/px/来源）

记录时间：2026-09-13（官方坐标全量校对：以官方 ROM 坐标为唯一基准修正 86 条，未收录单独列出）

---

## 2026-09-13 补：待定 8 条按官方名修正（截图对照确认）

- 用户上传游戏内地图截图 + 按「待定按官方改」指令，8 条名字待定全部改为官方名：
  塔邦达冻原→**塔邦挞大雪原**、东部荒漠→**东格鲁德**、迪普兰荒地→**埃文坡高地**、莱恩贝克岛→**杜艾斯岛**、
  海拉鲁城堡城镇遗址→**海拉鲁城邑遗迹**（CastleTownMark 官方译名）、大妖精泉×3→**大精灵之泉**（截图确认游戏内 4 泉同名）
- 修 build_official_fix.py：ALIAS `CastleTownMark→HyruleCastleTown`（SaveFlag 前缀剥离）+ 策略 A 同名条目加坐标距离校验（防 4 泉误配）
- 重跑比对：487 匹配 / 1 未收录（采石场遗址不动）/ 无需改 480 + 名字待定 7（坐标已对齐、仅 692 表缺中文，名字均为官方名）
- 验证：node --check + 浏览器运行时 8 条新名生效、4 个大精灵之泉、旧名全部消失、页面零报错
- 文件：`source/apply_pending_fix.py`（8 条改名脚本）、备份 `regions.js.bak3`

记录时间：2026-09-13（待定 8 条按官方修正，未收录仅剩采石场遗址）

---

## 2026-09-14 清理：其他 agent 调试残留 + 补提交核心数据源

- 用户反馈"其他 agent 做的东西效果不好"，经探查确认残留四类，已清理（全部走回收站，可恢复）：
  1. source/ 下调试/一次性脚本 38 个（debug_text1-10.py、dbg_*、check_*、extract_*、parse_*、unpack_*、msbt_reader 等）——已被 build_official_fix.py + apply_* 正式流程替代
  2. 中间产物 CSV/tsv 7 个（地名对照表/地名校对表/官方地名坐标对照表 394 旧版/r4地名匹配结果/缺失官方地名 等）——被最终版 CSV 取代
  3. 解包残留 source/cnzh/（7MB）+ source/extracted/（3.5MB）；备份 regions.js.bak/.bak2/.bak3
  4. .workbuddy/（WorkBuddy agent 记忆目录，有价值产物已并入项目）
- 用户资产移出：旷野之息地图1.jpg → H:\攻略\旷野之息\（项目根目录不放图片）
- **补提交核心数据源 `source/简体中文地名表.csv`**（692 条官方中文名，build_official_fix.py 依赖，此前未跟踪）
- 优化建议.md 两条（默认神庙+塔、favicon）此前已实现，文档本体保留待用户决定
- 保留不动：tools/（14 瓦片工具）、waypoint-data/（比对依赖）、source/ 已跟踪资产、botw-all-hoes/ object-map/（第三方忽略数据）

记录时间：2026-09-14（清理其他 agent 调试残留约 11MB，补提交核心数据源）

---

## 2026-09-14 进入提示地点数据整理（已实施归档）

- 拆解结论：官方"进入时提示"位置 = `Static.xml` LocationPointer 131 条（游戏内大地图不标、进入提示）；另有 LocationMarker 215 条（地图标记）
- 131 条中 80 条已在地图（r3/r4，含主殿/二殿/三殿），51 条缺失（含 1 条无名纯指针点已排除，实际 50 条）
- 已整理 `source/进入提示地点补充清单.csv`（51 条，可重跑 `source/build_r5_list.py`）：
  - **r5（地图放到最大后显示）**：城堡内部房间 11 条（观察室/食堂/训练所/图书馆/牢房/塞尔达的房间/船坞/塞尔达的研究室/国王的书房/东坑道/西坑道）
  - **r4（按既有规则显示）**：海拉鲁城堡本体 + 其他 39 条（桥梁12/遗迹村落15/入口门4/温泉泉2/其他6）
- ✅ 已实施：见下方「实施：进入提示地点补入地图」条目（regions.js 488→539、r5 渲染规则、验证通过、已推送）

记录时间：2026-09-14（进入提示地点数据整理备用）

---

## 2026-09-14 实施：进入提示地点补入地图（r5 最大缩放显示 + r4 补 40）

- `regions.js` 488 → **539 条**：新增 r4 40 条（海拉鲁城堡本体 + 桥梁12/遗迹村落15/入口门4/温泉泉2/其他6）+ r5 11 条（城堡内部房间）
- `map.js _drawRegions` 新增 **r5 渲染规则**：`showR5 = scale >= maxScale*0.95 && z>=7`（地图放到最大且最高清瓦片才显示城堡房间）；r4 在 r5 出现后弱化 0.75；r5 字号 base 7 / kMax 2.2
- `index.html` 版本号 bump：regions.js?v=rn4、map.js?v=4（绕缓存）
- 验证：node --check；浏览器运行时 539 条/层级分布正确/零报错；渲染对比——最大缩放显示观察室/塞尔达的房间/训练所/图书馆等 r5 房间，中等缩放仅显示 r4（主殿/二殿/三殿/海拉鲁城堡），符合设计
- 搜索适配无需改动（搜索索引基于 markers 不含地名）
- 文件：`source/apply_r5_list.py`（应用脚本）、备份 `regions.js.bak4`

记录时间：2026-09-14（r5 最大缩放显示实施完成，已提交推送）

---

## 2026-09-14 文档整理：游戏文件记录 + md 全量梳理

- 新增 **《游戏文件数据记录.md》**：本项目从游戏 ROM 用到的 4 项数据（LocationMarker.msbt 中文名 / Location.xml / Static.xml 坐标）、来源路径、坐标换算（pxX=2*gameX+12000）、数据流向图、可重跑步骤
- **README.md**：新增「文档导航」表 + 「官方数据核对 v2.1」小节（94 条修正 + 51 条补充 + 539 条层级）
- 修正过时文档：`RomFS目录地图.md`（第 7 节清理掉已删除的解包目录引用）、`source/官方地名提取指南.md`（重写为现存产物/脚本，保留 MSBT 格式要点）、`优化建议.md`（状态→已实现）、`架构.md`（regions 计数 r4=134/r5=11 共 539、地名系统表加 r5 行）
- 删除 `source/botw-all-hoes/README.md`（仅占位标题，目录本体保留）

记录时间：2026-09-14（文档整理已提交 f792e07）

---

## 2026-09-15 实施：live 悬浮框导航模块（TOTK 同款，第一批=神庙+塔+克洛格）

> 用户确认三点：① BOTWmap 新建 `live/` 模块、数据全部复用；② 网页实时层加到现有 `app/index.html`（不新建页面）；③ 第一批进度=神庙(136)+希卡塔塔(15)+克洛格(900)。

- **存档解析（关键突破）**：BOTW `game_data.sav` = 12 字节头 + 从 0x0C 起扁平表，每 8 字节 `[hash u32][value u32]`，hash = crc32(flag 名)（与 marcrobledo hashes.csv 一致）。vector3f 跨 3 个 8 字节块，读位置 = hash 偏移 +4/+12/+20。完成判定：神庙 `Clear_DungeonNNN`、塔 `MapTower_NN`、克洛格 `MainField_Npc_HiddenKorok*`（以逐 flag 为准，计数器仅参考）
- **定位链路**：存档锚点（playtime 最高槽，单锚点避免全图扫描）→ 内存扫描 7.6s → shortlist 监听 → 记住偏移（fast path 秒启）。坐标换算 `map px=(2X+12000, 2Z+10000)`，已验证 `数据.js` 3084 标点同系
- **live/ 模块（全部自研）**：`start.py`（8766 HTTP 服务：/pos /target /config /overlay/* /rescan + watchdog + 存档变更热重建进度）、`locate.py`（内存定位）、`progress.py`（存档解析、playtime 选槽）、`build_progress.py`（合并 → `data/progress_points.json`，1051 点：神庙 136 / 塔 15 / 克洛格 900）、`overlay.py`（TOTK 浮窗移植：BOTW WebP 瓦片 z≤7 原生+偏移 4384/6384、中文类别、1px=0.5m、左拖/滚轮/右键菜单/Esc）、`start-all.bat`（一键启动）
- **网页实时层（加到现有 app）**：`app/js/live.js` 新增——玩家红点+移动轨迹+导航线（点击标注→「导航到这里」）、状态胶囊（未连接/定位中/已连接）、存档进度同步（已收集标注自动变灰打勾）、统计面板新增「存档进度 · 游戏内」（神庙 99/136、塔 15/15、克洛格 50/900，真实存档数据）、图层勾选实时同步服务配置（浮窗读同一配置）。离线时全站静默降级
- **数据源**：marcrobledo `zelda-botw.hashes.csv`（45052 条）+ lud99 `botw-unexplored`（Data.cpp 900 克洛格/120 神庙/16 DLC，坐标 bijection 匹配）+ BOTWmap 自身标点
- **验证**：存档解析/坐标匹配（900/900、120/120、16 DLC、15/15）、内存定位（7.6s 扫描+fast path）、浮窗无头渲染（z5~z9 瓦片铺满）、网页浏览器实测（进度同步 164 点变灰、统计面板存档进度、状态胶囊）均通过
- **未推送**：提交 `67734a5`，待老大验证定位跟随（需在游戏里移动角色让 verified 翻转）后再推送
- 已知事项：data.js 神庙标记 137 = 136 真神庙 + 复苏神庙（剧情地点，无存档 flag）；克洛格计数器=74 与逐 flag=50 不一致，以逐 flag 为准

记录时间：2026-09-15（live 模块实施完成，已提交 67734a5，未推送）

---

## 2026-09-15 实施：存档上传解析 + 回忆/神兽并入进度（提交 4e9768e，未推送）

- **网页上传存档（纯前端）**：统计面板下方「📂 从存档加载进度…」+ 侧栏拖拽。FileReader 本地解析 `game_data.sav`（12 字节头 + 0x0C 起 [hash u32][value u32] 扁平表），对照 `data/save_flags.json`（1068 条：flag 哈希 → 标记，由 `live/build_save_flags.py` 生成，与个人存档无关，可随站部署）。文件不上传服务器，解析结果吸收进 localStorage（与 live 自动同步共用基底）。任何人（含部署站访客）可用。
- **回忆影片（主线 13 张）+ 神兽(4) 并入存档进度**：`IsGet_MemoryPhoto_000~012` ↔ 主线照片1~13（按标记名「照片N」映射，memory_no 是游戏回忆编号非照片编号）；神兽 `Clear_RemainsWater/Wind/Fire/Electric`（内部名 Remains，已验证 4/4）。build_progress.py 产出 1068 点（神庙136/塔15/克洛格900/回忆13/神兽4），done：回忆 12/13、神兽 4/4。浮窗 DONE_CATS 增加回忆影片/神兽。
- **统计面板按用户要求调整**：删「全部标注」行；「存档已同步 ✓」只显示状态不带数字（live 服务载入或本会话上传时显示）。
- **关键坑**：神庙点 hash 是 Location 哈希（进入/发现），完成判定用 Clear_DungeonNNN → 上传查找表必须用 done_hash，否则"进入未通关"的神庙被误计为完成（实测 100 vs 99，已修）；save_flags.json 相对路径须 `../data/`。
- **部署**：`data/progress_points.json`（个人进度快照）已 git rm 不入库（部署版由上传提供；本地由服务热重建）。`data/save_flags.json` 入库随站部署。
- 验证：浏览器实测上传真实存档 → toast「神庙 99 · 塔 15 · 克洛格 51 · 回忆 12 · 神兽 4（游玩 62.1 小时）」、统计面板 99/136 15/15 4/4 51/900 12/23、localStorage 181 条、控制台无页面报错。

## 2026-09-15 收尾：存档位置提示 + 帮助弹窗（提交 4dff3d0/2c9c06b，归档推送）

- 「从存档加载进度…」旁加 `?` 问号：悬停显示小气泡（存档文件名 + Ryujinx/Cemu 路径 + Win+R 方法）。
- 问号**点击**打开「使用帮助 · Q&A」弹窗：存档路径可选中复制（解决悬停消失无法复制问题）+ 进度同步原理 + 浮窗导航说明 + 常见问题（宝箱/英杰回忆无存档 flag、克洛格计数器仅供参考、手机端可上传）。关闭：× / 点弹窗外 / Esc。
- 离线状态胶囊文案改为「● 离线 · 可上传存档同步进度」（部署站访客场景）。
- 归档：本地全部提交推送 gitee + github。

## 2026-09-15 数据核对：botw-re-notes 权威 gamedata 表（提交）

- 拉取 leoetlino/botw-re-notes（2241 文件）至 `live/_research/botw-re-notes/`（第三方仓库，gitignore 不入库）。
- **核对结论（脚本 `live/_research/verify_re_notes.py` 可复跑）**：
  - `gamedata_by_reset_type.yaml` = 44767 个 flag（**自带 crc32 hash**，官方 gamedata 导出）—— 权威 flag 源，可作为公用数据库基准。
  - 与 marcrobledo hashes.csv（45052 条）：0 缺失、仅 5 个改名冲突（0.011%），两独立来源互相验证一致。
  - 克洛格 900 个 flag（Ground 567 + Fly 333）与 lud99 koroks.json 的 900 条 **100% 对齐、哈希 0 冲突**。
  - 项目 `app/data/save_flags.json` 1068 条 **全部** 存在于权威源。
  - 坐标换算复核：lud99 游戏坐标 → map px=(2X+12000, 2Z+10000) 与 progress_points 一致；世界坐标范围 X∈[-6000,6000]、Z∈[-5000,5000]（24000×20000 px，1px=0.5 单位）；areas.md 确认生态区域映射用 X∈[-5000,4999]、Z∈[-4000,4000]。
- **浮窗方案评估**：对比 DirectX Hook+ImGui / Windows 透明窗 / 外部窗口跟踪 → 现有实现（Tkinter 置顶半透明窗 + GetWindowRect 跟随 Ryujinx 贴边 + Canvas 小地图）即「Windows 透明窗」路线的轻量形态，最适配现状（不遮挡游戏、纯 Python 稳定、零注入）；Hook 方案脆弱且无必要，Overlayer 工具无增益；如需叠在游戏画面上再补 WS_EX_TRANSPARENT 点击穿透。

## 2026-09-15 修复：部署站查找表 404（提交后推）

- 部署站结构为「app/ 内容置于站点根」（botw.yalin.site），原 `../data/save_flags.json` 解析到根外 404，且 data/ 目录未随站点发布 → 上传存档报「标志查找表尚未就绪」。
- 修复：`data/save_flags.json` 移入 `app/data/save_flags.json`（git mv），build_save_flags.py 输出路径同步；save_upload.js 改为多路径兜底 fetch（`data/` → `../data/` → `./data/`），根部署与 app 子目录部署均可用。
- 验证：本地 8767（app/ 为根，模拟部署站）与 8766（/app/ 子目录）两种结构上传存档均成功。

## 2026-09-15：live 重构为 Go 版独立工具（提交 b3f0b67，已推双远端）

> 背景：Python 版 live 打包成 exe 约 20MB（解释器大头），用户希望"几百 K / 几 M 的绿色软件"。方案收敛：**Go 重写、只做网页角色追踪、砍游戏内浮窗 overlay**，在线地图页通过 CORS + PNA 跨域连本地 `127.0.0.1:8766`。

- **新目录 `live-go/`**（零第三方依赖，纯 syscall + 标准库）：`winapi.go`（OpenProcess/VirtualQueryEx/ReadProcessMemory/进程快照/guest RAM 块枚举）、`savefile.go`（存档解析 + playtime 选槽 + 同位置去重保留最高 playtime）、`known.go`（偏移记忆 fast path）、`locate.go`（锚点窗口扫描/分组打分/正交矩阵/64MB chunk 并发 ≤16）、`watch.go`（poll 10Hz / watchdog 3s / verifyKnown / watchShortlist / locateByMotion 兜底）、`server.go`（/pos /target /config /rescan + CORS + `Access-Control-Allow-Private-Network`）、`main.go`（找 ryujinx 进程、浏览器自动开在线站、监听 127.0.0.1:8766）
- **产物**：`灵墟地图追踪助手.exe` 6,976,000B（≈7MB，`-s -w` 裁剪）；`dist/灵墟地图追踪助手-分享版.zip` 2.8MB（exe+README+known_addrs）
- **前端**：`app/js/live.js` 4 处 API fetch 改 `http://127.0.0.1:8766`（LIVE_API），index.html `v=5→v=6`
- **验证全过**：单元测试（含真实存档对照 Python 基准 playtime=223431、korok=75、坐标一致）；真实 Ryujinx（ryujinx-canary 1.3.351 + BOTW 1.6.0 xci）fast path 秒连、`/pos` 10Hz 实时坐标（ok=true verified=true source=known）；**公网 HTTPS 部署站 → 本地 127.0.0.1 PNA 预检实测通过**（botw.yalin.site 状态胶囊 `● 1945,238 · 目标 59m`，手动 fetch ok=true）
- 分享方式：朋友下载仓库（或 `live-go/dist/灵墟地图追踪助手-分享版.zip`）→ 双击 exe → 自动开在线地图显示位置；Ryujinx 需先进入游戏。更多见根 README「角色追踪」小节与 `live-go/README.md`
- 遗留：GitHub Releases 未发（无 gh CLI/token，可网页手动上传 zip 发布）；旧 Python `live/` 保留作协议基准不维护

## 2026-09-15：Go 版补「存档收集进度热同步」（提交 acedd27/524b77b，已推双远端）

> 用户反馈：拿到克洛格果实地图没更新。原因：Go 版当时只做角色追踪，进度同步未移植。本次补齐，**游戏内保存后 ~1-2s 地图自动变灰**（非重读档）。

- `live-go/progress.go`（新）：参考表 `data/progress_points.json`（1068 条 t/hash/done_hash/坐标/名，由 `live/build_progress.py` 构建，入库随包分发）+ 每 2s 扫存档目录 mtime → 主档（最高 playtime）flag 现算 done → `/progress` 同构输出（points/counts/generated）；参考表缺失时 503，前端降级
- `server.go`：`/progress` 路由 + `/pos` 附带 `progressGen`；`main.go` 启动 watch goroutine
- 前端 `live.js`：`loadProgress` 优先 `LIVE_API+/progress`（失败降级静态文件），poll 检测 progressGen 变化立即重拉（限频 3s）；index.html `v=6→v=7`
- **验证全过**：单元测试 TestProgress（1068 点、总数 136/15/900/13/4、对照 Python 基准 done 只增不减）；真实存档 korok 51→54→55（与游戏内计数器 75→78→79 自洽）；在线站实测统计面板「克洛格果实 55/900 · 存档已同步 ✓」、无 404/CORS 错误
- 分发：zip 重新打包 2.8MB（exe+README+known_addrs+data/progress_points.json，参考表必须随包）
- 注意（写给用户）：BOTW 拿果实/神庙后需**游戏内触发一次保存**（手动存档/传送/睡觉/自动存档）才写存档文件，之后自动同步

---

## 2026-09-16：侧栏 UI 迭代 — 折叠功能 + 窗帘模式（提交 12f1f18/a0bad3b/cdc6774/736827d，已推双远端）

> 用户需求：桌面端侧栏可收起，收起/展开不影响右侧大地图；侧栏分组排版更紧凑；右下按钮不被遮挡。

- **初版折叠（12f1f18）**：flex 布局下 `#sidebar` width 300→0，把手贴地图区左缘，状态存 localStorage `botwmap.sidebarCollapsed.v1`，保留当前缩放中心不 fit。
- **体验调整（a0bad3b）**：
  - 去掉 width 过渡动画（用户嫌"地图拉伸一下"），瞬间切换
  - 把手从垂直居中竖条 → 贴侧栏右缘、与搜索框同高的文字按钮「‹ 收起」/「展开 ›」
  - 侧栏分组改两列：`buildLayerList` 把每个分组包进 `.lg-group[data-group]` + `.lrows`，CSS grid 2 列只对设施地点/敌人/任务生效，探索收集保持单列
  - 右下「●离线…」状态胶囊和「X/Z 坐标」读数从右下角移到地图底部居中（上下两行），右下 4 个缩放/跟随按钮独占右下角，🎯 跟随按钮不再被遮
- **排版修复（cdc6774）**：
  - 两列列宽仅 ~135px，lrow 横向 flex 里进度数字（min-width 52px）把 `.lname` 挤成 17px → 名字竖排换行（"村庄"→"村/庄"）。改 lrow 纵向两行：勾选框+名字一行（`white-space:nowrap`），进度数字换行到名字下方靠右小字
  - 收起时地图"先缩放一下再恢复"根因：click handler 延迟 240ms 才 resize canvas backing store，期间旧 backing store 被 CSS 拉伸显示。删掉 setTimeout，class 切换后同步 resize
- **窗帘模式（736827d，定版）**：
  - 上述同步 resize 仍有偏移（收起时 mapArea 宽度 502→802，中心保持逻辑有偏差）。用户要求"侧栏像窗帘，开关不影响后面地图"
  - `#sidebar` 从 flex item 改成 **absolute 浮层**（top:58 left:0 bottom:0 z-index:10），收起时 `translateX(-100%)` 整条滑出屏幕左侧；`#app` 从 flex-row 恢复 flex-column，`#mapArea` 始终占满全宽
  - 把手 left 随 body class 切换：展开贴侧栏右缘（x=300），收起贴屏幕左缘（x=0）
  - **效果**：收起/展开前后 canvas backing store 802×1033 完全不变，地图像素位置零偏移，只是窗帘掀/盖
  - 顺带修首帧 bug：侧栏改 absolute 后，首帧 rAF 时 flex 布局未算高，canvas backing store 停在默认 300×150（打开黑屏/小图）。加双 rAF + `window.load` 事件兜底
- 版本号 bump：style.css?v=10 / ui.js?v=8 / main.js?v=9 / map.js?v=7
- 验证：浏览器实测收起/展开 backing store 不变、两列排版单行不竖排、底部信息居中、右下按钮完整、刷新持久化

记录时间：2026-09-16（侧栏折叠+窗帘模式定版，已提交推送）
