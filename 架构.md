---
tags: [Web, 地图, 架构, 技术文档, 项目总结]
created: 2026-09-13
---

# BOTWmap-技术架构（游戏互动地图参考模板）

> 来源：BOTWmap（旷野之息互动地图，已上线 https://botw.yalin.site/，2026-09-12~13 验证闭环）。
> 用途：**以后做任何游戏地图（瓦片地图 / 点位标注 / 地名系统）直接以本文为架构参考**——数据层、引擎、渲染管线、地名系统、双端、部署全流程可复用。
> 副本：`<项目根目录>/BOTWmap/架构.md`（与本文同内容，供 agent 开工直接读）。

## 一、总体分层（通用模式）

```
数据层（静态 JS/JSON，零后端） → 引擎层（Canvas 瓦片地图引擎） → 交互层（UI 状态）
                                              ↓
                             渲染管线 draw()：瓦片 → 地名 → 测量 → 图标
持久化：localStorage（图层/样式/收集进度）   部署：纯静态托管（EdgeOne Pages）
```

**设计原则**：
1. 零构建零后端——数据预处理好（Python 离线构建），前端只做渲染与交互
2. **瓦片金字塔 + 按需加载**——大图不常驻内存（45MB 整图解码 = 1.4GB 是卡顿元凶）
3. **图标固定像素不随缩放**——参考 ali213 交互习惯
4. 地名/标注等语义层用 Canvas 矢量绘制（不烧瓦片）

## 二、目录与模块职责（BOTWmap 实例）

| 文件 | 职责 | 备注 |
|---|---|---|
| `index.html` | 入口骨架 + 内联 favicon + 按版本号引入 JS/CSS | `?v=` 版本化绕浏览器缓存（关键） |
| `css/style.css` | 布局：侧栏 / 弹窗 / 响应式 | 手机抽屉侧栏 |
| `js/data.js` | 点位全量数据（3084 点 / 28 类） | 静态 JS，坐标已换算 24000 逻辑系 |
| `js/regions.js` | 地名数据（r1=8 / r2=24 / r3=362 / r4=94） | 见第五节 |
| `js/main.js` | 启动入口：URL 参数(z/cx/cy/l)、搜索、测量、标记、分享 | 薄层 |
| `js/map.js` | **地图引擎核心**：瓦片加载/LRU/平移缩放/渲染管线/地名绘制 | 40KB，可复用性最高 |
| `js/ui.js` | 图层分组勾选、设置弹窗（地名样式）、localStorage 持久化 | 见第六节 |
| `assets/icons/` | 35 张游戏内图标 | 固定像素 |
| `assets/tiles/` | 瓦片金字塔 z1~z7（10096 张 WebP 41MB） | 见第三节 |

## 三、核心：瓦片地图引擎（通用设计）

| 要素 | 做法 |
|---|---|
| 瓦片金字塔 | 7 级：z1 全图 4 张 → z7=1:1 像素 7520 张（4/16/36/120/480/1920/7520） |
| 缩放映射 | `zoom = round(log2(scale)) + 7`，夹在 1..7；scale=1 即 1:1 |
| 按需加载 | 只请求当前视口（含少量 padding）内瓦片；平移/缩放时增量加载 |
| 瓦片缓存 | LRU（Map 上限 400 张 ≈70MB），异步加载完成 rAF 合并重绘 |
| 加载限流 | 并发上限 48（防手机网络打满） |
| 降级兜底 | 目标级瓦片未加载时用低一级撑画面（放大不空白） |
| 缩放上限 | `maxScale = 1`（手机/PC 一致，均可到 z7 最高清） |
| 体积优化 | PNG→WebP q82：10096 张 236MB→41MB，平均像素差 <3/255 不可辨 |

坐标统一：`gameX=(pxX-12000)/2`（24000 逻辑系 ↔ 游戏坐标）。

## 四、渲染管线（绘制顺序 = 图层 z 序）

```
draw() 每帧：
 1. tiles      —— 瓦片底图（按 zoom 级）
 2. regions    —— 地名文字（最底层语义，半透明底深字）
 3. measure    —— 测量线/标尺
 4. markers    —— 点位图标（最上层，固定像素）
 5. custom     —— 用户自定义标记
```
规则：**语义文字永远在瓦片之上、交互图标之下**——图标不能被地名挡住。

## 五、地名系统（通用方法论）

**分级 + 整数倍阈值 + 瓦片清晰度兜底**：

| 级 | 内容 | 起显 | 之后 |
|---|---|---|---|
| r1 | 大区（8） | 全图~2x | 2x 后隐藏 |
| r2 | 地形区（24） | 2x | r3 出现即隐藏 |
| r3 | 细地名（362） | 4x | r4 出现后**弱化保留**（α×0.8、字号封顶 1.7） |
| r4 | 更细（94） | 8x **且瓦片 z≥6** | 字号上限 2.0 |

- **字号封顶**（r1=1.5 / r2=r3=1.7 / r4=2.0）解决"层级文字过大"
- **z 兜底**：r4 要等瓦片清晰（z≥6）才显示，避免细字糊底
- 默认样式：米色底 `[242,232,213]`(bgA=0.6) + 深褐字 `[92,58,30]`(txA=0.92)，仿游戏内

**数据源三层（任何游戏地图通用）**：
1. **官方坐标源**（Location.xml 类）——取位置
2. **官方显示名源**（LocationMarker.xmsbt 类，ID→显示名）——取名
3. **第三方中文/本地化兜底 + MANUAL 手工修正表**——覆盖官方名缺失

⚠️ 坑：第三方中文名与坐标错位严重，**不能靠坐标最近匹配取中文名**；重名处理 = 同层取第一 + 坐标 <60px 旧名残留删除 + 低层排除上层已覆盖名。

## 六、数据流与状态持久化

- **URL 参数**（分享定位）：`?z=<放大倍数>&cx=<0-1>&cy=<0-1>&l=<图层>`——`l=` 优先于 localStorage（分享链接不受本地状态影响）
- **localStorage 键**：`botwmap.layers.v2`（图层勾选）、`botwmap.style.v2`（地名样式）、收集进度自动保存
- **交互流**：ui.js 改状态 → `map.invalidate()` → draw() 重绘；瓦片加载完成 → rAF 合并重绘
- **调试参数**：`?z=&cx=&cy=` 可直达任意缩放/位置（验证利器）

## 七、双端适配（R9 铁律反哺）

- 手机：侧栏抽屉式（☰ 展开 / 遮罩收起）、触摸手势（单指拖拽 / 双指捏合 / 双击放大）
- **缩放级数两端一致**（maxScale=1）——否则手机只能到低清瓦片、看不到最细地名
- 图标/地名字号用逻辑像素（不随设备 dpr 超分放大）
- 交付前必须过手机 + PC 双端验证

## 八、部署与构建

- 纯静态零后端 → EdgeOne Pages（Gitee main 自动部署，输出目录 `app/`），GitHub 为双远端备份
- 数据/瓦片离线构建（Python）：`tools/`（抓取/切瓦/转 WebP）+ `source/`（数据解析/重建脚本）
- 瓦片构建产物不入库（可由脚本重建，`waypoint-data/` 第三方克隆 .gitignore 排除）

## 九、做新游戏地图的行动清单（直接照做）

1. **找官方数据**：官方坐标源 + 官方显示名源（优先级高于任何第三方）
2. **瓦片**：抓取或整图切瓦 z1~z7（256px、WebP q82）
3. **图标**：游戏内素材，固定像素
4. **数据**：统一坐标系，静态 JS 导出（Python 离线构建）
5. **引擎**：复用 map.js 模式（瓦片金字塔 + LRU + 渲染管线）
6. **地名**：分级 r1~r4 + 整数倍阈值 + z 兜底 + 字号封顶
7. **双端**：抽屉侧栏 + 触摸手势 + 缩放一致
8. **验证**：浏览器自动化逐级断言 + 像素对比 + 控制台零报错
9. **部署归档**：EdgeOne + 双远端 + 知识库/进度.md 归档闭环

## 参考

- 线上：https://botw.yalin.site/；仓库 GitHub `yalincc/BOTWmap` + Gitee `yalincc/botwmap`
- 性能方案：[[BOTWmap-大图瓦片化性能方案]]
- 项目流程：[[BOTWmap-互动地图项目流程总结]]
- 协作纪律：`E:/WorkSpace/AI-CORE/RULES.md`（R1 先方案后动手 / R9 双端适配）
