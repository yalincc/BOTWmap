# 旷野之息 · 游戏文件数据记录

> 记录本项目**实际从游戏文件中提取并使用了哪些数据**、来源路径、提取产物与消费方，供数据溯源与后续更新。
> 分工：本文 = 本项目用到的游戏数据；`RomFS目录地图.md` = 游戏 RomFS 目录全图（找东西用）；`source/官方地名提取指南.md` = 官方地名的提取操作流程。

---

## 0. 游戏本体信息

| 项 | 值 |
|---|---|
| 游戏 | 《塞尔达传说：旷野之息》（Switch） |
| 版本 | **1.6.0**（最终版，含全部 DLC「英杰之诗」） |
| 载体 | XCI：`01007EF00011E000`（1G+1U+2D） |
| RomFS 提取 | Ryujinx 模拟器「提取 RomFS」→ `G:\YUZU\Switch Games\RomFS` |
| 简中语言包 | `Pack/Bootup_CNzh.pack`（14 语言包之一，文本在此层） |

---

## 1. 本项目用到的游戏数据（4 项）

| # | 游戏内文件 | 内容 | 提取产物（项目内） | 消费方 |
|---|---|---|---|---|
| 1 | `Pack/Bootup_CNzh.pack` → `Message/Msg_CNzh.product/StaticMsg/LocationMarker.msbt` | **官方简体中文地名表**（label → 中文名，692 条） | `source/LocationMarker.xmsbt`（原始文件拷贝）+ `source/简体中文地名表.csv`（692 条） | regions.js 中文名；`build_official_fix.py` 改名 |
| 2 | `Pack/Bootup.pack` → `Map/MainField/<格>/*.smubin`（Location） | **官方地点坐标**（地图标记 215 条） | `waypoint-data/Location.xml`（git 克隆 MrCheeze/botw-waypoint-map，仓库作者从游戏解包） | `build_official_fix.py` 坐标源 |
| 3 | `Pack/Bootup.pack` → `Map/MainField/<格>/*.smubin`（Static） | **官方地点坐标**（地图标记 + 进入提示 LocationPointer 131 条 + SaveFlag Location_*） | `waypoint-data/Static.xml`（同上克隆） | `build_official_fix.py` 坐标源；`build_r5_list.py` 补充清单源 |
| 4 | 第三方瓦片/图标 | 地图底图 z1~z7（10096 张）、游戏内图标（35 张） | `app/assets/tiles/` + `app/assets/icons/` | 产品底图与图标（源自 ali213 站点抓取，非 ROM） |

> 说明：`waypoint-data/` 是 `MrCheeze/botw-waypoint-map` 的 git 克隆（坐标已由其从游戏 smubin 解出并转成 XML），不是本机直接解包；本机直接解包的是**中文名 MSBT**（第 1 项）。

---

## 2. 坐标体系与换算

- 项目统一坐标系：**24000 × 20000 逻辑像素**（`app/js/data.js`、`regions.js` 同用）
- 官方坐标单位：游戏 X（横向）、Z（纵向）
- 换算公式（项目内 `map.js gameCoord()` 与各脚本同源）：

```
pxX = gameX × 2 + 12000
pxY = gameZ × 2 + 10000
```

| 名称 | 示例（海拉鲁城堡） |
|---|---|
| 游戏坐标 | X=11492 → (11492−12000)/2 = **−254**，Z=8777 → (8777−10000)/2 = **−611.5** |
| 地图像素 | (11492, 8777) |

---

## 3. 数据流向（游戏文件 → 产物 → 产品）

```
RomFS（1.6.0）
├─ Pack/Bootup_CNzh.pack ──► LocationMarker.msbt ──► source/LocationMarker.xmsbt
│                                                    └─► source/简体中文地名表.csv（692 条）
├─ Pack/Bootup.pack ──► Map/MainField/*.smubin ──► waypoint-data/Location.xml + Static.xml（git 克隆）
│
└─► source/build_official_fix.py ──► 官方地名坐标总表.csv（706 label）
    ├─► 官方修正清单.csv / 官方修正清单_应用前.csv ──► source/apply_official_fix.py ──► app/js/regions.js（488 条中 94 条按官方修正：86 + 8 待定）
    ├─► 官方未收录清单.csv（1 条：采石场遗址，官方无，保留原样）
    └─► source/apply_pending_fix.py（8 条待定名，2026-09-14 按官方改完）

Static.xml 的 LocationPointer（131 条进入提示点）
└─► source/build_r5_list.py ──► 进入提示地点补充清单.csv（51 条）
    └─► source/apply_r5_list.py ──► app/js/regions.js（+51 → 539 条：r4 补 40 + r5 城堡房间 11）
```

---

## 4. 项目内保留的相关产物

| 文件 | 说明 |
|---|---|
| `source/LocationMarker.xmsbt` | 官方 MSBT 原始拷贝（含 692 条官方简体中文地名） |
| `source/简体中文地名表.csv` | label → 简体中文（692 条，parse 产物） |
| `source/官方地名坐标总表.csv` | 官方坐标汇总（706 label，Location+Static 合并去重） |
| `source/官方修正清单.csv` / `_应用前.csv` | 与 regions.js 比对结果（86 条修正 + 8 条待定；应用前为基线） |
| `source/官方未收录清单.csv` | 官方没有、保留原样的条目（1 条） |
| `source/进入提示地点补充清单.csv` | LocationPointer 缺失 51 条（r5 城堡房间 11 + r4 其他 40） |
| `waypoint-data/Location.xml` / `Static.xml` | 官方坐标源（git 克隆，勿手改） |

---

## 5. 更新地名/坐标时怎么重跑

| 场景 | 步骤 |
|---|---|
| 游戏更新（重新提取 RomFS） | 重新解 `Bootup_CNzh.pack` 取 msbt → 更新 `简体中文地名表.csv`；重拉 waypoint-data 更新 XML |
| 重跑官方比对 | `python source/build_official_fix.py` → 核对 `官方修正清单.csv` → `python source/apply_official_fix.py` |
| 重跑进入提示清单 | `python source/build_r5_list.py` → `python source/apply_r5_list.py` |
| 全部脚本幂等，可反复重跑；改前自动备份 regions.js |

---

*数据源与口径：本文数据来自本机 RomFS 解包与 waypoint-data 克隆，核对结论见 `官方地名核对.md`。*
